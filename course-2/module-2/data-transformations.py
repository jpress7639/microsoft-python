# MERGING 
# Merging allows you to combine data frames based on common columns or indices.

# CONCATENATING
# Stacking data frames on top of each other row-wise or column-wise
# It's helpful when you have data from different sources that you want to combine into a single data frame.

# SORTING 
# Arranges your data in a specific order 

# GROUPING 
# Grouping allows you to split your data into groups based on a specific column or set of columns 

# pd.merge() - go to for merging data frames
# you'll need to specify which direction you wish to merge (inner, outer, left, or right, columns, or indices)

# pd.concat() - to concatenate

# df.sort_values() - sort a dataframe by one or more columns

# df.groupby() - method allows you to group a data frame by one or more columns 

# df.agg() - allows you to apply one or more aggregation functions to the data frame or a group of dataframes
# Common aggregation functions include - mean, sum, count, min, and max

# Define what you want to achieve
# Document your steps - keep track of transformations
# validate your results by checking transformations product expected output
# experiment and iterate 

